# 시청 기록의 내 채팅·일반 후원과 채널 순위 — 2026-09-19

## 범위와 저장 계약

- 브랜치 `1.3.9`, 시작 HEAD `52d7d3536811dc0cde223fdc7a5adac67955571e`. 기존 미커밋 작업을 유지했다. 버전 변경·커밋·push·릴리스는 하지 않았다.
- 시청 기록에 채널별 누적 시청 시간 순위, 전체 기간 라이브 목록, 내 채팅·일반 후원의 내용과 날짜별 집계를 추가했다. 같은 채널의 겹친 시청 구간은 한 번만 합산하며 정확한 구간이 없는 과거 기록은 저장된 시간을 유지한다.
- `liveWatchHistoryChatEnabled`, `liveWatchHistoryDonationEnabled`는 각각 기본 꺼짐이며 부모 옵션 `liveWatchHistoryEnabled`를 따른다. 전송 확인 직후 기존 단일 writer로 저장하며 최소 시청 시간·영상 재생 여부와 관계없이 수집한다. 최소 시청 시간에 미달하면 `appendActivities`로 활동만 저장하여 시청 시간을 부풀리지 않는다.
- `watchActivityPage.js`는 실제 채팅 행의 React 메시지를 읽기만 한다. `watchActivity.js`가 로그인한 작성자와 현재 라이브·수집 시작 시각을 확인하고, `liveWatchHistory.js`가 기존 세션 스냅샷에 활동을 실어 보낸다. 기록 변경은 기존 background 큐와 `shared/watchHistoryStore.js`의 단일 writer만 수행한다.
- 활동은 확인된 `live:` ID에만 반영한다. 작성자 해시·서버 작성 시각·종류로 중복을 구분하여 재전송·같은 방송의 여러 탭·DOM 재마운트가 개수를 늘리지 않는다. 동일 문구의 서로 다른 작성 시각은 별도 채팅이다.
- 저장 전 대기는 세션별 최근 500개다. 내용과 중복 구분용 ID는 방송별 500개, 전체 3,000개를 보관한다. 오래된 내용을 정리해도 방송의 날짜별 횟수·치즈 합계는 유지한다. 정리한 시각 이하의 늦은 재관측은 다시 집계하지 않는다. 기록 삭제 barrier는 늦은 스냅샷의 복원을 막는다.
- 개인 활동은 기존 시청 기록처럼 이 브라우저의 `chrome.storage.local`에만 저장한다. 채팅 전송·결제·후원을 실행하지 않는다. 새 권한이나 원격 실행 코드는 추가하지 않았다.

## 치지직 현재 응답·소스·DOM 확인

확인일: 2026-09-19, 비로그인 별도 Chromium 프로필. 공개 라이브는 `https://chzzk.naver.com/live/3497a9a7221cc3ee5d3f95991d9f95e9`에서 확인했다. 특정 채널 ID나 빌드 파일명은 런타임 선택 조건에 넣지 않았다.

- 현재 웹 앱 파일: `https://ssl.pstatic.net/static/nng/glive/resource/p/static/js/index-DdGx9_UO.js`.
- 라이브 채팅 DOM은 `aside#aside-chatting [role='log'] > [class*='_wrapper_'] > [class*='_item_']`. 공개 라이브에서 13개 행과 `__reactProps$*.children.props.chatMessage`를 확인했다.
- 실제 일반 채팅의 `user`는 문자열, `time`은 epoch millisecond 숫자, `type`은 `1`, `status`는 `NORMAL`, `originalContent`는 문자열이었다. 렌더링용 HTML인 `content`를 저장하거나 실행하지 않는다.
- 웹 앱은 자신이 보낸 채팅도 전송 응답의 `msgTime`으로 메시지를 만든다. 본인 프로필 여부는 로그인 상태의 `userIdHash`와 메시지 `user`를 비교한다. 닉네임이나 DOM에 잠깐 남은 배지를 기준으로 판별하지 않는다.
- 현재 소스에서 후원 메시지 `type: 10`, 일반 후원 `extras.donationType: CHAT`, 수량 `extras.payAmount`, 익명 여부 `extras.isAnonymous`를 확인했다. 익명·미션·미션 참여·파티·구독은 수집하지 않는다. **실제 후원 전송이나 로그인한 본인 후원은 실측하지 않았다.** 후원 fixture는 현재 소스의 필드와 분기를 바탕으로 만들었다.
- 로그인 상태 API는 `https://comm-api.game.naver.com/nng_main/v1/user/getUserStatus`다. 실제 비로그인 응답은 HTTP 200, `code: 200`, `content.loggedIn: false`, `content.userIdHash: null`이다.
- 위 API는 `Origin: https://chzzk.naver.com`에 `Access-Control-Allow-Origin: https://chzzk.naver.com`, `Access-Control-Allow-Credentials: true`를 반환했다. 실제 확장의 isolated context에서도 credentials 포함 요청이 HTTP 200으로 성공했다. 추가 호스트 권한 없이 페이지에 허용된 CORS를 사용한다.

## 검증

- 자동 회귀: 본인 판별·과거 메시지 제외·지원 후원 유형·DOM 재사용/재마운트·옵션 끄기·SPA 경계·늦은 인증 응답 취소·최소 시청 시간·일시정지 중 활동 저장·재전송/여러 탭 중복·보관 상한·삭제 후 재생성 방지·채널 순위·필터·본문의 안전한 텍스트 표시를 확인했다.
- 즉시 저장 후속 변경의 관련 테스트 39개가 통과했다. 최초 기능 추가 때 실행한 관련 기존 테스트 158개도 통과했다.
- 최종 `npm.cmd run test:all`: 840개 통과. `npm.cmd run lint`, `npm.cmd run format:check` 통과.
- 로컬 화면 검증용 기록으로 실제 Chromium에서 시청 기록 페이지 1280px/390px, 라이트/다크와 설정창 390px 라이트/다크를 렌더링했다. 가로 넘침과 page error가 없었고 스크린샷으로 줄바꿈·배치·색상·더 보기 버튼의 숨김을 확인했다. 화면 검증용 기록은 사용자 저장소에 넣지 않았다.
- 별도 임시 프로필에서 저장소의 확장을 로드하여 실제 확장 ID `oijgeclpcafhkbaameifdomjononepol`, manifest 1.3.9, 새 content script 등록을 확인했다. 라이브 새로고침 후 이름이 `치즈 스패너`인 isolated execution context에 `BetterChzzk.watchActivity.createCollector`가 로드됐고, 실제 확장 시청 기록 페이지와 두 옵션을 확인했다. page error는 없었다.

## 남은 수동 확인과 배포 문구

- 사용자 프로필의 로그인 계정으로 채팅을 보내고 저장되는 흐름과 실제 일반 후원 기록은 미검증이다. 공개 비로그인 프로필에서 로그인된 것으로 가정하거나 테스트 후원을 실행하지 않았다.
- 사용자는 확장을 다시 로드하고 라이브 탭을 새로고침한 뒤, 설정 → 시청 기록에서 필요한 저장 옵션을 켜고 옵션을 저장한다. 평소 작성한 채팅의 전송이 확인된 뒤 열린 시청 기록 페이지에 바로 나타나는지 확인한다. 최소 시간 미달 또는 일시정지 중에는 시청 시간이 늘지 않고 `활동만 기록`으로 나타난다. 실제 후원 검증을 위해 새 후원을 할 필요는 없으며 추후 평소 일반 후원 내역으로 확인할 수 있다.

## 같은 날 후속 요청: 채팅 즉시 반영

- 이전 구현의 최소 시청 시간과 15초 저장 주기 대기로 채팅 직후 확인할 수 없던 경로를 회귀 테스트에서 먼저 재현했다. 활동을 감지하면 즉시 mutation을 보내고, 최소 시간 미달 활동은 시청 세션·시청 시간을 추가하지 않는 별도 mutation으로 처리했다.
- 열린 시청 기록 페이지의 2초 갱신 대기를 제거했다. 같은 이벤트 묶음은 한 번만 읽으며, 먼저 시작한 오래된 읽기 응답이 최신 표시를 되돌리지 않도록 세대를 확인한다.
- 기록 삭제와 경합하는 새 세션의 활동은 수집 세션 시작 시각으로 보존 여부를 판별한다. 기존 시청 기록·구형 누적 기록은 활동만 추가해도 보존하며, 활동만 있는 방송도 조회·삭제할 수 있다.
- 실제 확장을 로드한 별도 Chromium 프로필에서 **검증용 로그인 응답·서버 확인 채팅 fixture**로 시험했다. 최소 저장 시간 10분, 재생하지 않은 영상에서 첫 채팅의 저장·열린 기록 페이지 반영까지 43ms를 관측했다. 이는 해당 검증 실행의 측정값이며 일반적인 지연 보장이 아니다.
- 두 번째 채팅 직후 라이브 페이지를 바로 새로고침하고, 시청 기록 페이지도 다시 새로고침하여 채팅 보존을 확인했다. 일반 후원 fixture까지 총 3개 활동이 저장됐고 시청 시간은 0초였다. MAIN marker, 실제 확장 ID, 라이트/다크 390px 화면의 가로 넘침 없음, page error 없음도 확인했다. 실제 계정으로 채팅을 보내거나 후원을 실행하지 않았다.
- 과거 내역 복원, 채팅이 DOM에 나타나지 않은 시간, 로그인 조회 실패, 익명·미션·외부 후원과 구독 결제는 포함되지 않는다. 표시 합계는 확인된 내역의 집계이며 결제 영수증이나 전체 지출 내역이 아니다.
- 스토어 배포 전 설명과 개인정보 공개 문구에 **선택적으로 내 채팅 내용·작성자 해시·작성 시각 및 확인된 일반 후원 치즈·횟수를 로컬 저장한다는 점**, 기본 꺼짐, 보관 상한과 삭제 방법을 반영해야 한다. `README.md`와 `PRIVACY.md`는 갱신했으며 스토어 문구 자체는 게시하지 않았다.

## 같은 날 후속 요청: 라이브 내역에서 내 채팅 펼치기

- 각 방송의 `내 채팅 ○개 보기` 버튼으로 그 방송의 저장된 채팅을 바로 펼친다. 작성 시각과 본문을 최근 순으로 표시하며 선택한 기간·날짜를 적용한다. 시청 세부 보기와는 각각 열고 닫을 수 있다.
- 내용은 처음 50개를 표시하고 `채팅 더 보기`로 50개씩 추가한다. 누적 채팅 수와 보관된 내용 수를 구분하고, 수집 내역 없음·과거 내용 정리 상태를 실제 저장값에 따라 안내한다.
- 새 활동 저장·정렬·목록 갱신 시 방송별 펼침 상태, 표시 범위와 키보드 포커스·목록 스크롤 값을 유지한다. 해당 방송을 삭제하면 펼침 상태도 정리한다. 본문은 HTML이 아닌 텍스트로 표시한다.
- 관련 페이지·활동 테스트 22개가 통과했다. 방송별 독립 동작, 날짜 필터, 안전한 본문 표시, 더 보기, 새 활동 반영 중 포커스·스크롤 유지, 삭제와 빈 목록 상태를 검증했다.
- 실제 확장을 로드한 별도 Chromium 프로필의 검증용 기록으로 Enter 키 펼침, 50→55개 표시, 새 활동 반영 후 56개와 포커스 유지, 접기, 다른 방송의 빈 목록을 확인했다. 라이트/다크 1280px·390px에서 가로 넘침과 page error가 없었다. 이 변경은 저장된 기록의 조회 UI에만 적용하며 수집 방식·개인정보 항목은 바꾸지 않는다.

## 같은 날 후속 요청: 메시지 없는 일반 후원 집계

- 최초 제외 지점은 `features/watchActivityPage.js`의 본문 타입 검사였다. 빈 문자열은 통과했지만 `originalContent`가 `null`이거나 없는 후원은 종류·금액을 확인하기 전에 제외됐다.
- 본인·작성 시각·정상 상태·일반 후원 유형·양의 정수 치즈를 먼저 검증하고, 그 후원에 한해 nullish 본문을 빈 문자열로 정규화한다. 일반 채팅의 본문 타입 검사, 익명·미션 등 제외 조건과 중복 식별자는 유지했다. 저장소의 단일 writer나 스키마는 바꾸지 않았다.
- 2026-09-19 14:07 KST에 치지직 페이지와 현재 [웹 앱 소스](https://ssl.pstatic.net/static/nng/glive/resource/p/static/js/index-DdGx9_UO.js)를 새로 조회했다. 일반 후원 요청의 `donationText`는 선택적인 본문 값이고, 메시지 변환기는 수신 본문을 `originalContent`에 그대로 옮긴다. 치지직의 후원 표시도 빈 본문과 금액을 별도로 처리한다. 로컬에 받은 소스는 실행하지 않고 읽기만 했다.
- 같은 날 공개 라이브 DOM에서는 현재 React 메시지 경로와 일반 채팅 8개를 확인했으나, 검사 시점에 일반 후원 행은 없었다. 실제 후원의 원시 payload에서 null을 직접 관측한 것으로 보고하지 않는다. 빈 문자열·null·필드 누락 fixture는 확인한 선택적 본문 계약과 오류 분기를 검증한다.
- 수정 전에는 새 회귀 테스트에서 null·누락 후원 2건이 빠지고, 즉시 저장 통합 테스트에서 null 본문 후원의 저장을 기다리다가 실패했다. 수정 후 관련 46개 테스트가 통과했다. 빈 문자열·null·누락 본문의 금액·횟수, 재전송 중복, 시청 시간 미증가, 잘못된 본문 타입·익명·타인·차단 상태·지원하지 않는 유형·잘못된 수량 제외를 확인했다. 통합 테스트는 본문 없는 후원이 즉시 저장되고 기록 페이지에 `메시지 없는 후원`으로 표시되는지 확인한다.
- 별도 `empty-donation-profile`에 실제 확장을 로드한 뒤, 검증용 로그인 응답과 후원 메시지 fixture로 MAIN 브리지 → isolated 수집기 → background writer → 열린 시청 기록 페이지를 확인했다. 실제 확장 ID는 `oijgeclpcafhkbaameifdomjononepol`이다. 최소 시청 시간 10분·일시정지 상태에서 후원 4건, 10,000치즈, 시청 시간 0초가 저장됐고 중복 재관측은 합계를 늘리지 않았다. 라이브와 기록 페이지를 새로고침해도 내역이 유지됐다. 라이트·다크 390px에서 본문 대체 안내·금액·횟수, 가로 넘침 없음과 page error 없음을 확인했다.
- 실제 계정으로 후원하거나 결제 요청을 보내지 않았고, 사용자 프로필의 기록을 수정하지 않았다. 이미 누락되어 저장되지 않은 과거 후원을 자동 복구하는 변경은 아니다. 적용하려면 확장을 다시 로드하고 라이브·시청 기록 탭을 새로고침한다.
- 브라우저 증거는 임시 경로 `C:/Users/neder/AppData/Local/Temp/cheese-spanner-history-20260919/`의 `empty-donation-browser.json`, `empty-donation-light.png`, `empty-donation-dark.png`에 남겼다.
- 최종 전체 테스트 844개, lint, 전체 format:check가 통과했다. 최초 동시 검증에서는 범위 밖 카테고리 검색 스크롤 테스트가 시간 제한으로 한 번 실패했다. 해당 테스트를 단독 실행하고 브라우저 검증 종료 후 전체 검사를 다시 실행하여 모두 통과했으며, 범위 밖 코드나 테스트의 시간 제한·검증 조건은 변경하지 않았다. 최종 로그는 `empty-donation-tests-final.log`다.
