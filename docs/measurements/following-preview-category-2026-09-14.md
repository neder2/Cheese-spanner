# 팔로잉 미리보기 카테고리 링크 — 2026-09-14

## 현재 응답과 이동 주소

- 브랜치: `1.3.7`, 기준 HEAD: `b2f22a2`.
- 2026-09-14 23:14 KST에 `https://api.chzzk.naver.com/service/v2/channels/<channelId>/live-detail`을 조회했다.
- `7ce8032370ac5121dcabce7bad375ced`: `categoryType: "GAME"`, `liveCategory: "Marvels_Wolverine"`, `liveCategoryValue: "마블 울버린"`.
- `0f9a3b4fbb0e7137d1f0b4d70563031c`: `categoryType: "ETC"`, `liveCategory: "talk"`, `liveCategoryValue: "talk"`.
- 치지직 홈의 실제 링크에서 `/category/GAME/Marvels_Wolverine/lives`, `/category/ETC/talk/lives`를 확인했다. 네이티브 마블 울버린 링크를 클릭해 첫 번째 주소로 이동하는 것도 확인했다. 타입의 대소문자를 바꾸지 않는다.
- 현재 다크 테마에서 `--sem-color-border-neutral-alpha-base`, `--sem-color-surface-neutral-alpha-weakest`, `--sem-color-surface-brand-alpha-weaker`를 확인해 최초의 테두리형 링크에 사용했다.

## 구현 범위

- 기존 미리보기 조회 응답의 타입·ID로 현재 탭에서 여는 카테고리 링크를 만든다. 별도 카테고리 조회나 라우터 패치는 추가하지 않는다.
- 필요한 필드가 없거나 잘못된 경우에는 기존 카테고리 이름만 표시한다. 표시 이름으로 주소를 추측하지 않는다.
- 카테고리와 채널명 링크는 같은 원본 행 정체성 검사를 사용한다. 숨겨지거나 제거된 미리보기, 다른 채널로 재사용된 원본 행에서는 이동을 막는다.
- 테두리·배경·호버·키보드 포커스를 제공하고, 긴 이름은 줄바꿈한다. 수정 키와 가운데 클릭은 일반 링크 동작을 유지한다.
- 후속 요청으로 배경·테두리를 치지직 그린, 글자를 어두운 색으로 변경했다. 기존 멀티뷰 조작부에서 사용하는 브랜드 배경·반전 본문 토큰을 재사용하며 기본값은 `#00ffa3`·`#0e0f10`이다. 최소 높이는 28px에서 24px로, 상하 패딩은 3px에서 1px로 줄였다. 긴 이름은 여전히 여러 줄로 늘어날 수 있다.
- 진행 시간은 정보 줄 오른쪽 끝에 정렬하고 13px·600 굵기로 표시한다. 기존 제목과 같은 라이트 `#111114`·다크 `#F2F4F7` 색을 사용하며, 앞의 구분점은 제거했다. 링크가 없는 긴 카테고리도 시간을 밀어내지 않도록 말줄임한다.

## 검증 범위

- 관련 자동 검사 33개 통과: GAME·ETC 주소, 기존 조회 재사용, 정상 링크 클릭, 잘못된 이동 정보, 원본 행 재사용, 기존 채널명 링크, 재생과 정리 동작.
- 진행 시간은 JSDOM에서 라이트 → 세 가지 기존 다크 루트 상태 → 라이트 전환 시 색·크기·굵기와 정렬 CSS 적용을 확인했다. 이는 실제 브라우저의 배치·글꼴 렌더링 검증을 대신하지 않는다.
- 전체 `npm.cmd run test:all` 717개, lint·전체 format:check·변경 파일 diff 검사 통과.
- 실제 사이트의 데이터와 이동 주소를 확인한 것이며, 새 확장을 다시 로드한 상태의 미리보기 클릭·라이트/다크 화면·좁은 너비는 아직 실브라우저 미검증이다.
- 수동 확인: 확장 관리에서 다시 로드하고 치지직 탭을 새로고침한다. 라이트·다크 모드에서 팔로잉 미리보기의 진행 시간 대비와 오른쪽 정렬, 카테고리 테두리·긴 이름·Tab 포커스를 확인하고, 일반 클릭과 Ctrl/Command·가운데 클릭으로 해당 카테고리 방송 목록에 이동하는지 확인한다.
