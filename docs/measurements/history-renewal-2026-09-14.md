# 시청 기록 UI 리뉴얼 — 2026-09-14

## 범위

- 브랜치 `1.3.7`, 시작 HEAD `b2f22a2b6e36ea3c1402c3b22580b3f644c3f4a2`.
- 기존 작업 트리의 옵션창·시청 기록 변경 위에서 요청 범위만 수정했다. 버전·커밋·릴리스는 변경하지 않았다.
- 옵션창의 `options-brand.css`를 시청 기록에서도 로드해 로컬 Pretendard 글꼴을 공유한다. 기존 테마 색상과 18px 카드, 캡슐형 검색창을 사용한다.
- 검색·정렬, 목록 새로고침, 항목 선택, 전체 삭제를 용도에 따라 배치했다. 전체 삭제가 모든 달에 적용됨을 명시했다.
- `이 달 전체 보기`는 날짜 필터만 해제한다. 검색어·정렬·항목 선택은 유지하고 이전 날짜 버튼으로 포커스를 반환한다. 날짜의 선택 상태와 오늘을 ARIA로 제공한다.
- 좁은 화면에서 요약과 선택 도구가 줄바꿈되도록 조정했다. 스타일 변경은 시청 기록 페이지 범위이며 옵션창의 기존 글꼴 선언만 공유한다.

## 자동 검증

- 관련 페이지 테스트 141개 통과.
- 최종 `npm.cmd run test:all`: 698개 통과, 실패·건너뜀 0개.
- `npm.cmd run lint`, `npm.cmd run format:check` 통과.
- 변경 파일 `git diff --check` 통과.
- 날짜 필터 해제의 검색·정렬·선택 보존, 포커스 반환, 월 이동 시 버튼 비활성화, 저장소 변경 요청이 없음을 회귀 테스트로 확인했다.

## 실브라우저 미검증

Chrome 탭 목록에서 `chrome-extension://oijgeclpcafhkbaameifdomjononepol/history.html`을 확인했으나, 브라우저 도구의 URL 보안 정책이 페이지 접근을 거부했다. 우회 접근은 하지 않았다.

실제 확장 다시 로드·주입 상태, 라이트/다크 화면, 좁은 화면 및 200% 확대, 실제 저장 기록의 배치와 콘솔 오류는 확인하지 못했다. CSS와 DOM의 정적 점검 및 JSDOM 테스트를 시각 검증으로 간주하지 않는다.

수동 확인: Chrome 확장 관리에서 치즈 스패너를 새로고침한 뒤 시청 기록 탭을 새로고침한다. 라이트/다크와 좁은 창에서 검색·정렬·날짜 선택·이 달 전체 보기·세부 펼치기·선택 해제를 확인한다. 전체 삭제 위치와 안내만 확인하며 실제 기록 삭제는 필요하지 않다.
